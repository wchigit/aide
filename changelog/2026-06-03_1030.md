---
- title: Fix Copilot SDK initialization in packaged builds
  type: bug-fix
  author: aide
  description:
    - Resolve the Copilot CLI binary robustly with existence checks and fallback paths
    - Surface the real SDK startup error in chat instead of a generic "not initialized" message
    - Ensure macOS x64 + arm64 release builds bundle the matching Copilot CLI binary
